{
  "typeName": "AWS::XRay::TransactionSearchConfig",
  "description": "This schema provides construct and validation rules for AWS-XRay TransactionSearchConfig resource parameters.. Source:- https://github.com/aws-cloudformation/aws-cloudformation-rpdk.git",
  "sourceUrl": "https://github.com/aws-cloudformation/aws-cloudformation-rpdk.git",
  "properties": {
    "IndexingPercentage": {
      "$ref": "#/definitions/IndexingPercentage"
    }
  },
  "definitions": {
    "AccountId": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^\\d{12}$"
        },
        {
          "$ref": "../../components/cf.functions.json#/Aws_CF_FunctionString"
        }
      ],
      "description": "User account id, used as the primary identifier for the resource",
      "title": "AWSXRayTransactionSearchConfigAccountIdDefinition"
    },
    "IndexingPercentage": {
      "description": "Determines the percentage of traces indexed from CloudWatch Logs to X-Ray",
      "type": "number",
      "minimum": 0,
      "maximum": 100,
      "title": "AWSXRayTransactionSearchConfigIndexingPercentageDefinition"
    }
  },
  "tagging": {
    "taggable": false
  },
  "additionalProperties": false,
  "readOnlyProperties": [
    "/properties/AccountId"
  ],
  "primaryIdentifier": [
    "/properties/AccountId"
  ],
  "type": "object",
  "title": "AWSXRayTransactionSearchConfigProperties"
}