{
  "name": "json-schema",
  "version": "0.0.0",
  "private": true,
  "description": "JSON schemas for Serverless Framework v4 (AWS) configuration files",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "test": "node --stack-size=8000 validate.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0"
  }
}
